<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Permission</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #080808 0%, #777979 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .form-card {
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .form-card:hover {
            transform: scale(1.05);
            box-shadow: 0 10px 20px rgba(0,0,0,0.2);
        }
        .header-gradient {
            background: linear-gradient(to right, #e60321, #2d0353);
            color: white;
        }
        .form-control:focus {
            border-color: #8a2be2;
            box-shadow: 0 0 0 0.2rem rgba(138, 43, 226, 0.25);
        }
        .logo1{
            display: flex;
            margin-bottom: 30rem;
            justify-content: center;
            padding-left: 1rem;
        }
    </style>
</head>
<body>
    <div class="logo1 mx-auto ">
        <a href="/" class="logo" style="font-size: 3.2rem;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        color: rgb(241, 236, 236);
        cursor: pointer;
        text-decoration: none;">ZELLBURY</a>
      </div>
    <div class="container" style="margin-left: -18rem; margin-top: 5rem;">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card form-card" id="form3">
                    <div class="card-header text-center header-gradient py-4">
                        <h2 class="card-title mb-0" id="formTitle">Admin Permission</h2>
                    </div>
                    <div class="card-body">
                        <form id="authForm3" method="post" action="/permission">
                            <div id="usernameGroup" class="mb-3">
                                <label for="username" class="form-label text-danger text-bold">Username</label>
                                <input type="text" class="form-control" id="username" name="username" placeholder="Enter creative username" required>
                            </div>
                            <div class="mb-3">
                                <label for="password" class="form-label text-info">Password</label>
                                <input type="password" class="form-control" id="password" name="password" placeholder="Create a strong password" required>
                            </div>
                            <div id="errorMessage" class="alert alert-danger mb-3" style="display: none;"></div>
                            <button type="submit" class="btn btn-danger w-100 py-2" id="submitButton">Submit Permission</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>